#include <bits/stdc++.h>

using namespace std;

int arr[101];
const int MIN = -1e9;

int maxLen(int idx, int start, int arr[], int arrSize) {
    if (idx > arrSize) {
        return MIN;
    }

    int ans;

    if (start == -1)

    return max()
}

int main() {

    int arraySize;

    cin >> arraySize;

    for (int i = 1; i <= arraySize; ++i) {
        cin >> arr[i];
    }

    sort(arr + 1, arr + arraySize + 1);



    return 0;
}